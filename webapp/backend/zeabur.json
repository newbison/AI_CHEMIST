{
  "$schema": "https://raw.githubusercontent.com/zeabur/zeabur/main/schema.json",
  "build": {
    "command": "cd ../frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt"
  },
  "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8001}"
}
